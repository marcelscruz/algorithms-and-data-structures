// --- Directions
// Given a string, return true if the string is a palindrome
// or false if it is not.  Palindromes are strings that
// form the same word if it is reversed. *Do* include spaces
// and punctuation in determining if the string is a palindrome.
// --- Examples:
//   palindrome("abba") === true
//   palindrome("abcdefg") === false

// Solution 1
// Use one of the solutions from reversestring and compare str to the reversed string

// Solution 2
// Turn into array and use every to compare the opposite characters
// Not 100% efficient because it passes the middle of str.length when comparing

function palindrome(str) {}

module.exports = palindrome
